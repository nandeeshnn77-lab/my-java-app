replicaCount: 2

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

container:
  name: myapp
  port: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"

  limits:
    cpu: "500m"
    memory: "512Mi"
